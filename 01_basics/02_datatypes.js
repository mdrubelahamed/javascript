"use strict"; // treat all JS code as newer version

/*
it's tells the browser to use the newer/modern version of js, like in html <!DOCTYPE html>
*/

// alert(3 + 3)



/*
  js data type = string, number, boolean, null, undefined, symbol(defined uniqueness generally), object
*/
const myName = "Md Rubel Ahamed" // string
let age = 21 // number
let isWorking = true // boolean
let nullType = null 
/* it's a null value means empty not zero
  like let's say i ask for a weather from a server, he can't send the value at time so
  at this time i want my temperature value should be null not 0 becaues 0 is also a temperature.
*/

let dream; // undefined

// console.log(typeof null); // object
// console.log(typeof undefined); // undefined

// console.log(typeof Symbol); // function
// console.log(typeof Symbol()); //symbol